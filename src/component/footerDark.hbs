{{#with data}}
<div class="footer_pages">
    {{#each pages}}
    <a href="{{path}}" class="nav_link">{{name}}</a>
    {{/each}}
</div>
<div>
    <span class="footer_copyright">{{copyright}}</span>
</div>
{{/with}}